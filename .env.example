AWS_ACCESS_KEY_ID=changeme
AWS_SECRET_ACCESS_KEY=changeme
AWS_DEFAULT_REGION=us-east-1

# Domain the edge Service will be pointed at once the live run starts (used to
# render the A-record instructions and the canary baseline docs). Not needed
# for local development.
GHOSTHREF_DOMAIN=changeme.example
